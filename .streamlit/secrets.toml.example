# Copy this file to `secrets.toml` (same directory) and fill in your key.
# `secrets.toml` is gitignored. On Streamlit Community Cloud, paste the same
# key into the Settings -> Secrets UI instead of committing the file.

OPENAI_API_KEY = "sk-..."
